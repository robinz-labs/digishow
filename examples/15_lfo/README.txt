LFO (Low-Frequency Oscillator)
==============================

animation.dgs
Uses LFO waveforms to drive signal outputs and create animations.

random.dgs
LFO can also generate random numbers (noise) to drive signal outputs.

timecode.dgs
Uses the timecode generated by LFO to trigger actions or change signal outputs over time.
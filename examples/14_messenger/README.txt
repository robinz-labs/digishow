Serial and TCP/UDP Messenger
============================

rfid_trigger.dgs
Connects to an RFID reader or barcode scanner via serial port, and triggers specific media playback based on received code messages.

gcode_sender.dgs
Connects to a G-Code device (e.g., multi-axis motion controller) via serial port, and sends specific code commands to the device.

